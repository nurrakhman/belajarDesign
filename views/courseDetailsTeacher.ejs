<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/scripts/chart.js"></script>
    <title>Course Details</title>
</head>
<body>
    Video Name  : <%= data.name %> <br>
    Duration    : <%= time(data.duration) %> <br> 
    Video Url   : <%= data.videoUrl %> <br>
    Updated at  : <%= data.createdAt %> <br>
    
    list of Students
    <ul>
    <% data.studentCourse.forEach((e,i)=>{ %>
    <li><%= e.username %> </li>
   <% }) %> 
</ul>
    description: <%= data.description %> 
    <div id="script" class="chart-container" style="position: relative; height:400px; width:400px" data=<%-JSON.stringify(chart)%>>
        <canvas id="myChart" width="60" height="60"></canvas>
    </div>
<script>
    let data = <%-JSON.stringify(chart)%>;
    const ctx = document.getElementById('myChart');
    const myChart = new Chart(ctx, {
    type: 'pie',
    data
});
myChart.resize(200, 200);
</script>

</body>
</html>